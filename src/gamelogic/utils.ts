export const getRandomInt = (min: number, max: number): number =>
  Math.floor(Math.random() * (max - min + 1)) + min;

export const Y_SIZE = 10;
export const X_SIZE = 15;
export const MINES_COUNT = 20;
